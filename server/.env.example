DATABASE_URL="postgresql://user@localhost:5432/merge_dev"
JWT_SECRET="change-me"
PORT=5050
APP_URL=http://localhost:3000
# GEMINI_API_KEY=
# BREVO_API_KEY=
# EMAIL_FROM="Merge <hello@example.com>"
# CRON_SECRET=   # protects /api/cron/trial-emails (Vercel Cron sends it as a bearer token)
# FEEDBACK_NOTIFY=feedback@example.com   # where the in-app Feedback form is emailed
# STRIPE_SECRET_KEY=rk_live_...       # restricted key with Customers, Checkout, Subscriptions, Billing Portal, Prices read
# STRIPE_WEBHOOK_SECRET=whsec_...      # from the webhook endpoint in Stripe
# STAFF_EMAILS=you@example.com   # comma-separated; can comp workspaces at /app/staff
